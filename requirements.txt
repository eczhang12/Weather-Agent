# Official OpenAI Python client used by `agent/agent.py` to call the LLM.
openai

# Loads API keys from a local `.env` file into environment variables.
python-dotenv

# HTTP client used by `agent/tools.py` to call the OpenWeatherMap API.
requests
